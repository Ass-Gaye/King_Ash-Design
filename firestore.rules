rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // By default, deny all direct client-side read/write access.
    // All access goes through the secure server-side API proxy.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
